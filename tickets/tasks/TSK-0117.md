---
actual_hours: null
assignees: []
created_at: '2025-08-20T11:11:55.577192'
dependencies: []
description: "**DELEGATION TO: Engineer Agent**\n\nFix the GitHub CLI commands in\
  \ `src/ai_trackdown_pytools/utils/github_utils.py` that incorrectly use `--json`\
  \ flag with create operations.\n\n**Specific Issues to Fix:**\n1. `create_issue()`\
  \ method (lines 102-103): `gh issue create --json` is invalid\n2. `create_pr()`\
  \ method (lines 267-268): `gh pr create --json` is invalid\n\n**Required Implementation\
  \ Pattern:**\n```python\n# Step 1: Create without --json\nresult = subprocess.run(['gh',\
  \ 'issue', 'create', ...], capture_output=True, text=True)\n\n# Step 2: Extract\
  \ number from URL in stdout\ndef extract_number_from_github_url(url_text):\n   \
  \ # Parse GitHub URL to get issue/PR number\n    # Format: \"https://github.com/owner/repo/issues/123\"\
  \n    \n# Step 3: Get JSON details with separate command\njson_result = subprocess.run(['gh',\
  \ 'issue', 'view', str(number), '--json', 'number,title,url'], capture_output=True,\
  \ text=True)\n```\n\n**Implementation Requirements:**\n- Add URL parsing utility\
  \ function\n- Update both `create_issue()` and `create_pr()` methods\n- Maintain\
  \ same return format as current methods\n- Add proper error handling for both commands\n\
  - Ensure subprocess calls handle text encoding correctly\n\n**Expected Deliverables:**\n\
  - Fixed `create_issue()` method\n- Fixed `create_pr()` method  \n- New URL parsing\
  \ helper function\n- All methods return consistent JSON format\n- Proper error handling\
  \ and logging\n\n**File Location:** `src/ai_trackdown_pytools/utils/github_utils.py`"
due_date: null
estimated_hours: null
id: TSK-0117
labels: []
metadata: {}
parent: null
priority: high
status: open
tags: []
title: 'DELEGATED: Engineer - Fix GitHub CLI --json flag error'
updated_at: '2025-08-20T11:11:55.577192'
---

# DELEGATED: Engineer - Fix GitHub CLI --json flag error

## Description
**DELEGATION TO: Engineer Agent**

Fix the GitHub CLI commands in `src/ai_trackdown_pytools/utils/github_utils.py` that incorrectly use `--json` flag with create operations.

**Specific Issues to Fix:**
1. `create_issue()` method (lines 102-103): `gh issue create --json` is invalid
2. `create_pr()` method (lines 267-268): `gh pr create --json` is invalid

**Required Implementation Pattern:**
```python
# Step 1: Create without --json
result = subprocess.run(['gh', 'issue', 'create', ...], capture_output=True, text=True)

# Step 2: Extract number from URL in stdout
def extract_number_from_github_url(url_text):
    # Parse GitHub URL to get issue/PR number
    # Format: "https://github.com/owner/repo/issues/123"
    
# Step 3: Get JSON details with separate command
json_result = subprocess.run(['gh', 'issue', 'view', str(number), '--json', 'number,title,url'], capture_output=True, text=True)
```

**Implementation Requirements:**
- Add URL parsing utility function
- Update both `create_issue()` and `create_pr()` methods
- Maintain same return format as current methods
- Add proper error handling for both commands
- Ensure subprocess calls handle text encoding correctly

**Expected Deliverables:**
- Fixed `create_issue()` method
- Fixed `create_pr()` method  
- New URL parsing helper function
- All methods return consistent JSON format
- Proper error handling and logging

**File Location:** `src/ai_trackdown_pytools/utils/github_utils.py`

## Details
- **Status**: open
- **Priority**: high
- **Assignees**: None
- **Tags**: None
- **Created**: 2025-08-20 11:11:55
- **Updated**: 2025-08-20 11:11:55

## Tasks
- [ ] Add task items here

## Notes
_Add any additional notes or context here._
